Este directorio contiene las reglas IDS/IPS locales del entorno.

Archivo activo:
- `local.rules`: firmas de laboratorio para DVWA, LDAP, honeypot y trafico DMZ hacia backend.

Ideas de ampliacion:
- reglas para escaneos con `nmap`
- deteccion de fuerza bruta web y SSH
- firmas de exfiltracion o beaconing
